#!/bin/bash

# archy:summary=Returns true if any of the named packages are missing from the system (or false if they're all there).
# archy:args=<packages...>

for pkg in "$@"; do
  if ! pacman -Q "$pkg" &>/dev/null; then
    exit 0
  fi
done

exit 1

