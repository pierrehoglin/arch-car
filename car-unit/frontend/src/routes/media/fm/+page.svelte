<script lang="ts">
  /* Placeholder. Nothing is wired to the daemon. */
</script>

<div class="pending">
  <span class="eyebrow">FM</span>
  <p>Tuning, presets and RDS -- see the fm command.</p>
</div>

<style>
  .pending {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--spacing-s);
    margin: auto;
    text-align: center;
  }

  p {
    margin: 0;
    font-size: 14px;
    color: var(--text-faint);
  }
</style>
